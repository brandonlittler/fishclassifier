# Copy to .env and set your values. Do not commit .env.
# Required for kubernetes/deploy.sh and kubernetes/create-secret.sh:
SUBSCRIPTION_ID=
ACR_NAME=
RESOURCE_GROUP=
# Optional (defaults shown):
NAMESPACE=fish-classifier
SECRET_NAME=acr-secret
